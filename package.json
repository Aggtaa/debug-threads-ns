{
  "name": "debug-threads-ns",
  "version": "0.2.1",
  "description": "Debug namespace structure helper coupled with thread-context",
  "main": "dist/index",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc -p .",
    "prepublishOnly": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Aggtaa/debug-threads-ns.git"
  },
  "keywords": [
    "continuation-local-storage",
    "debug",
    "context"
  ],
  "author": "Anton Zykov",
  "license": "MIT",
  "dependencies": {
    "thread-context": "^0.1.1"
  },
  "devDependencies": {
    "@types/node": "^16.7.10",
    "typescript": "^4.4.2"
  },
  "files": [
    "dist"
  ]
}
